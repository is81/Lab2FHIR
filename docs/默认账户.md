# 默认账户

> 首次部署后使用以下账户登录。**正式环境请务必修改密码。**

| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| `admin` | `admin123` | 病理科 | 导入报告 + 查看全部 |
| `doctor` | `doctor123` | 医生 | 仅查看报告 |

## 修改密码

```bash
cd backend
PYTHONIOENCODING=utf-8 python -c "
import sys; sys.path.insert(0, 'src')
from src.db.models import SessionLocal
from src.db.user_models import User
from src.auth.security import hash_password

db = SessionLocal()
user = db.query(User).filter(User.username == 'admin').first()
user.password_hash = hash_password('新密码')
db.commit()
print(f'{user.username} 密码已更新')
db.close()
"
```
